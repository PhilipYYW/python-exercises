from replit import clear


x = input() # 4
f = 1


if x.isdigit():
    x = int(x)
    for i in range(1,x+1):
        f = f * i
        print(f)
        clear()
  
    print(f)


else:
    print(f"{x} is an invalid type, try to input another data.")
