from replit import clear
s = 'Here are UPPERCASE and lowercase chars.'

d = {}
length = len(s)

for char in s:
    for n in range(1,len(s)+1):
        if char == s[n-1]:
           print(char, n)
           clear()
           break
    d[char] = n



print(d)
